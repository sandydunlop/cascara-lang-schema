/// Rules are created by the Schema Compiler when a rule keyword is encountered.
/// Rules have a `validate` method that is called by performValidation in the Structural Editor,
package io.github.qishr.cascara.schema.rule;
